name: CI - Triagem Service

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - develop
    tags:
      - 'triagem-v*'
    paths:
      - 'triagem-service/**'
      - '.github/workflows/ci-triagem.yml'
  pull_request:
    branches:
      - main
      - develop
    paths:
      - 'triagem-service/**'
      - '.github/workflows/ci-triagem.yml'

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./triagem-service

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt 
          pip install pytest pytest-cov httpx

      - name: Run tests and coverage
        run: |
          python -m pytest tests/ --cov=app --cov-report=xml

      - name: Build Docker image
        run: docker build --tag triagem-service:${{ github.sha }} .

      - name: Upload coverage
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report-triagem
          path: ./triagem-service/coverage.xml

  sonarcloud:
    if: github.event_name != 'pull_request' || github.actor != 'dependabot[bot]'
    needs: build-and-test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download coverage
        uses: actions/download-artifact@v4
        with:
          name: coverage-report-triagem
          path: ./triagem-service

      - name: SonarCloud Analysis
        uses: SonarSource/sonarqube-scan-action@v8.1.0
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN_TRIAGEM }}
        with:
          args: >
            -Dsonar.projectKey=danielibatista_triagem-final_triagem-service
            -Dsonar.organization=danielibatista
            -Dsonar.projectName=triagem-service
            -Dsonar.sources=triagem-service/app
            -Dsonar.tests=triagem-service/tests
            -Dsonar.python.coverage.reportPaths=triagem-service/coverage.xml

  publish-image:
    if: github.event_name == 'push'
    needs: build-and-test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/triagem-service
          tags: |
            type=sha,prefix=
            type=ref,event=branch
            type=match,pattern=triagem-v(.*),group=1

      - name: Build and publish Docker image
        uses: docker/build-push-action@v6
        with:
          context: ./triagem-service
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
